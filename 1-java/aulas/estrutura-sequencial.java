class Main {
    public static void main(String[] args) {
        double base, altura, area;

        // area = (base * altura) / 2; // como é sequencial, não se pode fazer calculos sem antes declarar valores para as variáveis.

        base = 10;
        altura = 4;

        area = (base * altura) / 2;

        System.out.println(area);
    }
}
